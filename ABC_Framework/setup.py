from setuptools import setup, find_packages

setup(
   name='ABC_Audit_and_validation1',
   version='0.2',
   description='My Package 2',
   author='MyName',
   author_email='My.email@example.com',
   packages=find_packages(),
   install_requires=[]
)
